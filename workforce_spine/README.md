# PROMETHEUS Workforce Spine Packet

This directory is an execution handoff generated from the PROMETHEUS Janus Prime deadline warboard.

## Active object

- Packet: `WSP-PROMETHEUS-20260719-EDGE-001`
- META-FATE: `MF-20260719-MF01-464`
- State: `READY_FOR_EXECUTION`
- Build truth: `PROMETHEUS_WORKFORCE_SPINE_PACKET_READY`

## What changed

The original deadline tracker is now backed by:

- a dual-surface router for ChatGPT Work and Codex
- sparse role activation
- a machine-readable 57-task queue
- unknown-task synthesis through `X-###` packets
- approval and proof classes
- 24 adversarial edge-case trials
- a receipt/ledger contract with JSON Schemas
- a PowerShell installer and Codex launcher

## Run

From PowerShell:

```powershell
Set-ExecutionPolicy -Scope Process Bypass -Force
& ".\Invoke-PROMETHEUS-WorkforceSpine.ps1" -RepoRoot "C:\Ghost\PROMETHEUS-V-1.1.1" -CopyWorkPrompt
```

Use `-PrepareOnly` to install the packet into the repository without launching Codex.

For ChatGPT Work, attach the enriched workbook and JSON packet, select **Work**, and paste `CHATGPT_WORK_LAUNCH_PROMPT.md`.

## Done means

Acceptance passed, evidence exists, receipt emitted, state updated, and the claim remains bounded by proof.
