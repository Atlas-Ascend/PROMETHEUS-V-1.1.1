# PROMETHEUS HERMES AGENT ZERO HYDRA CONVERGENCE

## Status
ACTIVE CAMPAIGN DIRECTIVE

## Parent truth
This directive extends, but does not replace:

- `build_truth/00_CANON_LOCK.md`
- `build_truth/01_GHOST_ATLAS_GRAND_SLAM.md`
- `build_truth/02_BUILD_TRUTH.md`
- GitHub Issue #1

## Existing-organ binding

- PROMETHEUS remains the public Command-to-Proof product and governing campaign prompt.
- AIS-Ω remains the bounded variation organ.
- JANUS/ODIN reconstructs current truth and selects the next executable obligation.
- PACKET OS decomposes missions into executable packets and evidence contracts.
- WORKFORCE SPINE activates only the roles needed for the current packet.
- METAFORGE generates materially different candidates.
- EDEN is the Windows/local execution and operator body.
- CALI remains the conversational operator surface.
- HERMES is the resident local coding intelligence served through Ollama.
- AGENT ZERO is the containerized interactive agent workspace and optional multi-agent operator surface, configured to use Hermes through Ollama.
- HEPHAESTUS executes real commands and captures real artifacts.
- HYDRA-UNO supervises runtime health, checkpoints state, restarts failed lanes, prevents duplicate consequential execution, and resumes from durable checkpoints.
- ADVERSARIAL TWIN attacks the provisional winner independently.
- SECA/DEVOS arbitrates evidence and controls promotion.
- PROOFGRID emits and verifies receipts, hashes, manifests, and claim support.
- THOTH/CANON-Ω preserves lineage and chronology.
- CAPABILITY GENOME preserves proven reusable operating intelligence.
- MEDUSA reverse-verifies claims and scrubs secrets.
- SERVERFORGE emits sanitized structured events and builds the case study.
- ARIADNE proves clean-room install and judge reproducibility.
- OLYMPIAN CONSOLE exposes truthful current state.
- ROADBRIDGE and CROWNGRID preserve guarded mobile/operator access.
- GARI records resource and execution budgets.

## Runtime topology

1. Windows EDEN owns the authoritative local repository and Git credentials.
2. VirtualBox Ubuntu supplies a recoverable Linux runtime.
3. Ollama runs inside Ubuntu and serves Hermes on guest port `11434`.
4. Agent Zero runs inside Docker in Ubuntu and uses Hermes through `host.docker.internal:11434`.
5. Windows reaches Hermes through NAT port `11435` and Agent Zero through NAT port `5080`.
6. The PROMETHEUS/HYDRA supervisor runs from EDEN against the authoritative Windows repository, using Hermes for bounded tool-driven implementation.
7. Every mutation is checkpointed, tested, challenged, receipted, committed, and pushed from the authoritative local repository.

## 200-role institutional office

The campaign must generate exactly 200 inspectable role definitions across 20 existing-organ divisions, 10 roles per division. Roles are capability definitions, not a false claim of 200 simultaneous autonomous workers. AIS activates only the roles required by the current mission packet. Implementation authority and promotion authority remain separate.

## Autonomous repair law

HYDRA may automatically:

- restart the VM;
- restart Ollama;
- restart Agent Zero;
- retry transient network/model operations with bounded exponential backoff;
- resume an interrupted PROMETHEUS phase from its latest durable checkpoint;
- rerun affected tests;
- reject or roll back a failed candidate using Git-preserved state;
- continue every unblocked task.

HYDRA may not:

- force-push;
- delete repository history;
- erase evidence;
- expose credentials;
- claim success without executable evidence;
- bypass a required human decision involving accounts, money, publication, or irreversible external action.

## Command-to-Proof closure sequence

1. Canon and branch convergence
2. 200-role office generation
3. Mission and packet reconstruction
4. Three materially distinct candidates
5. Isolated candidate execution
6. Evidence-backed rejection of weak routes
7. Provisional leader selection
8. HYDRA continuity event
9. Independent Adversarial Twin challenge
10. SECA promotion block
11. Repair and retest
12. ProofGrid receipt and hash manifest
13. Capability Genome generation
14. Recursive self-build
15. Second-task genome reuse
16. MEDUSA reverse verification
17. Package, install, doctor, demo, rollback, uninstall, reinstall
18. ARIADNE clean-room reproduction
19. ServerForge case study and truthful Olympian status
20. Release receipt, tag, push, and submission package

## Completion law

The campaign reaches `PROVEN` only when the original Build Truth completion law is satisfied with real execution evidence. A phase reaching its step budget is `PARTIAL`, never `PROVEN`. A concrete external dependency is `BLOCKED`, while every other unblocked lane continues.
